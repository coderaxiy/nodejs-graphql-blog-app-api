mutation Login($body: LoginUserDto!) {
  login(body: $body) {
    errors {
      field
      message
    }
    user {
      id
      firstName
      lastName
      username
      createdAt
      updatedAt
    }
  }
}
